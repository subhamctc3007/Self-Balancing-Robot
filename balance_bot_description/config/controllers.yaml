controller_manager:
  ros__parameters:

    update_rate: 1000 # gazebo's update rate

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    diff_drive_controller:
      type: diff_drive_controller/DiffDriveController

diff_drive_controller:
  ros__parameters:

    left_wheel_names:
      - left_wheel_joint

    right_wheel_names:
      - right_wheel_joint

    wheel_separation: 0.2
    wheel_radius: 0.05
    publish_rate: 50.0
    base_frame_id: base_link
    odom_frame_id: odom
    enable_odom_tf: true

    # cmd_vel_topic: /cmd_vel  # This does not work, had to implement a twist to stamped adapter 
    # use_stamped_vel: false   # no such params in jazzy version

    cmd_vel_timeout: 0.5